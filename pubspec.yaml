name: lab4
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Ensuring compatibility with Flutter's stable releases

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0 # Moved out of flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
